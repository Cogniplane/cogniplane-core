import type { AppConfig } from "../config.js";

const baseTestConfig: AppConfig = {
  API_HOST: "127.0.0.1",
  API_PORT: 3001,
  API_ORIGIN: "http://localhost:3000",
  DATABASE_URL: "postgres://postgres:postgres@localhost:5432/cogniplane",
  LOCAL_DEV_USER_ID: "test-user",
  ADMIN_USER_IDS: ["test-user"],
  LOG_LEVEL: "info",
  TRUST_PROXY: "1",
  SESSION_TITLER_CLAUDE_MODEL: "claude-haiku-4-5-20251001",
  SESSION_TITLER_TIMEOUT_MS: 2000,
  RUNTIME_GATEWAY_BASE_URL: "http://localhost:3001",
  RUNTIME_IDLE_TIMEOUT_MS: 300_000,
  RUNTIME_TURN_TIMEOUT_MS: 20 * 60 * 1000,
  RUNTIME_TOKEN_TTL_MS: 24 * 60 * 60 * 1000,
  // Must satisfy the loadConfig invariant TOOL_CONTEXT_TTL_MS >
  // RUNTIME_TURN_TIMEOUT_MS + APPROVAL_REQUEST_TTL_MS (20 + 10 min here) —
  // this fake bypasses validation, so a stale value here would hide a config
  // that the real loader rejects.
  SESSION_EXECUTION_LEASE_MS: 30_000,
  SESSION_EXECUTION_HEARTBEAT_MS: 5_000,
  TOOL_CONTEXT_TTL_MS: 35 * 60 * 1000,
  APPROVAL_REQUEST_TTL_MS: 600_000,
  ARTIFACT_STORAGE_BACKEND: "local",
  ARTIFACT_STORAGE_ROOT: "/tmp/cogniplane-artifact-tests",
  SKILL_BUNDLE_STORAGE_ROOT: "/tmp/cogniplane-skill-tests",
  SKILL_BUNDLE_STORAGE_BACKEND: "local",
  SKILL_BUNDLE_BUCKET_PREFIX: "",
  SKILL_BUNDLE_CACHE_ROOT: "/tmp/cogniplane-skill-cache-tests",
  ARTIFACT_BUCKET_REGION: "us-east-1",
  ARTIFACT_BUCKET_PREFIX: "",
  ARTIFACT_BUCKET_FORCE_PATH_STYLE: false,
  ARTIFACT_MAX_UPLOAD_BYTES: 25 * 1024 * 1024,
  MAX_REQUEST_BODY_BYTES: 2 * 1024 * 1024,
  ARTIFACT_DOWNLOAD_TTL_MS: 900_000,
  SKILL_BUNDLE_RETENTION_DAYS: 30,
  SESSION_TRASH_RETENTION_ENABLED: true,
  SESSION_TRASH_RETENTION_DAYS: 30,
  SKILL_MARKETPLACE_CACHE_TTL_MS: 300_000,
  PDFTOTEXT_BINARY_PATH: "pdftotext",
  RATE_LIMIT_WINDOW_MS: 60_000,
  SESSION_CREATE_LIMIT_PER_USER_PER_WINDOW: 10,
  SESSION_CREATE_LIMIT_PER_TENANT_PER_WINDOW: 50,
  MESSAGE_LIMIT_PER_USER_PER_WINDOW: 20,
  MESSAGE_LIMIT_PER_TENANT_PER_WINDOW: 100,
  ARTIFACT_UPLOAD_LIMIT_PER_USER_PER_WINDOW: 20,
  ARTIFACT_UPLOAD_LIMIT_PER_TENANT_PER_WINDOW: 100,
  PROJECT_FILE_WRITE_LIMIT_PER_USER_PER_WINDOW: 20,
  PROJECT_FILE_WRITE_LIMIT_PER_TENANT_PER_WINDOW: 100,
  SCHEDULED_JOB_CREATE_LIMIT_PER_USER_PER_WINDOW: 10,
  SCHEDULED_JOB_CREATE_LIMIT_PER_TENANT_PER_WINDOW: 50,
  OAUTH_CALLBACK_LIMIT_PER_USER_PER_WINDOW: 20,
  OAUTH_CALLBACK_LIMIT_PER_TENANT_PER_WINDOW: 100,
  ADMIN_QUERY_LIMIT_PER_USER_PER_WINDOW: 300,
  ADMIN_QUERY_LIMIT_PER_TENANT_PER_WINDOW: 1000,
  AUTH_ORGANIZATIONS_LIMIT_PER_USER_PER_WINDOW: 20,
  AUTH_ORGANIZATIONS_LIMIT_PER_TENANT_PER_WINDOW: 100,
  TURN_QUOTA_PER_USER_PER_DAY: 200,
  TURN_QUOTA_PER_TENANT_PER_DAY: 1000,
  PROJECT_FILE_RETENTION_ENABLED: true,
  SCHEDULER_ENABLED: false,
  SCHEDULER_POLL_INTERVAL_MS: 30_000,
  SCHEDULER_MAX_CONCURRENT_JOBS: 2,
  SCHEDULER_JOB_TIMEOUT_MS: 300_000,
  SCHEDULER_MAX_CONSECUTIVE_FAILURES: 5,
  PII_SCAN_MAX_CONCURRENT_JOBS: 2,
  SCHEDULED_JOB_MAX_ACTIVE_PER_USER: 50,
  AUTH_MODE: "dev-headers" as const,
  COGNIPLANE_ALLOW_DEV_HEADERS_ON_NON_LOOPBACK: false,
  DATA_ENCRYPTION_SECRET: "test-encryption-secret-must-be-at-least-32chars!",
  MCP_UPSTREAM_TIMEOUT_MS: 30_000,
  MCP_UPSTREAM_MAX_RESPONSE_BYTES: 4 * 1024 * 1024,
  JWT_SECRET: "test-jwt-secret-must-be-at-least-32chars!",
  JWT_KEY_ID: "test-key",
  JWT_VERIFICATION_KEYS: {},
  E2B_TEMPLATE_ID: "tpl-test-template",
  E2B_SANDBOX_TIMEOUT_MS: 30 * 60 * 1000,
  DEEP_AGENTS_EXECUTE_TIMEOUT_MS: 2 * 60 * 1000,
  PII_PROVIDER_ENABLED: false,
  PII_LLM_BASE_URL: "https://openrouter.ai/api/v1",
  PII_LLM_MODEL: "google/gemini-2.5-flash",
  PII_LLM_WIRE_FORMAT: "openai",
  PII_LLM_DISABLE_THINKING: false,
  UTILITY_LLM_ENABLED: false,
  UTILITY_LLM_WIRE_FORMAT: "ollama",
  UTILITY_LLM_DISABLE_THINKING: false,
  UTILITY_LLM_TIMEOUT_MS: 10_000,
  PII_PROVIDER_TIMEOUT_MS: 5000,
  PII_ARTIFACT_MAX_BYTES: 5 * 1024 * 1024,
  PII_CSV_PREVIEW_ROWS: 10,
  PII_CSV_PREVIEW_MAX_BYTES: 4096,
  PII_BREAKER_ENABLED: false,
  PII_BREAKER_FAILURE_THRESHOLD: 5,
  PII_BREAKER_WINDOW_MS: 60_000,
  PII_BREAKER_COOLDOWN_MS: 30_000
};

export function createTestConfig(overrides: Partial<AppConfig> = {}): AppConfig {
  return { ...baseTestConfig, ...overrides };
}
