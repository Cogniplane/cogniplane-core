{
  // package.json exports dist for both development and production consumers.
  // Rebuild after source edits before testing or typechecking either app.
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src/**/*.ts"]
}
